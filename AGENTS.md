 # Makefile
 - Run `make test` to test changes.
 - When you are done making changes, run `make fmt`.
 - Do not ever run any of the other `make` commands like `make migrate` or `make deploy` on your own unless explicitly asked to do so.
